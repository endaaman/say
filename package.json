{
  "private": true,
  "scripts": {
    "start": "pm2 start server.coffee --watch --name say-server --ignore-watch=\", settings.json\""
  },
  "dependencies": {
    "child-process-promise": "^1.1.0",
    "coffee-script": "^1.10.0",
    "koa": "^1.1.2",
    "koa-router": "^5.3.0",
    "request": "^2.67.0"
  }
}
